---
title: "R Notebook"
output: html_notebook
---

```{r}
library(mlr3verse)
library(ggplot2)

data_train <- read.csv("train.csv")
data_test <- read.csv("test.csv")
str(data_train)
```
```{r}
# plot the target variable response
ggplot(data_train, aes(x = y)) + geom_histogram(bins = 30, fill = "blue", color = "black") + labs(title = "Distribution of target variable y", x = "y", y = "Frequency")
```

